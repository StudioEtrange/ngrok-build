test:
  pre:
    - ./stella-link.sh bootstrap
    - ./ngrok-build.sh prepare
    - ./ngrok-build.sh build -d domain.com
    
  post:
    - if [ ! -d /home/ubuntu/ngrok-build/release ]; then exit 1; fi
